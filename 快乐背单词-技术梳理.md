% 快乐背单词 — 技术栈 / 功能 / 数据库 全梳理
% 技术梳理
% 2026-09-16

# 一、整体技术栈

| 层级 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 框架 | React | 19 | 单页应用 |
| 语言 | TypeScript | ~6.0 | 强类型 |
| 构建 | Vite | 8 | base 路径 `/vocab-app/` |
| 样式 | Tailwind CSS | 3.4 | 含暗色模式、自定义 brand 色系、自定义动画(fade-in/slide-up/flip) |
| 状态管理 | Zustand | 5 | 4 个 store:useStore / useAuth / dailyGoalStore / studyPlanStore |
| 工具库 | clsx | 2.x | className 拼接 |
| 图标 | lucide-react | 1.34 | 导航图标 |
| 后端(BaaS) | Supabase | supabase-js 2.112 | 鉴权 + PostgreSQL 数据库 + RLS 行级安全 |
| 本地存储 | Dexie / IndexedDB | 4.4 | 纯本地模式的数据层(目前 README 说仅 Supabase,但代码仍保留双写 fallback) |
| 文件解析 | pdfjs-dist 6 / mammoth 1.12 / xlsx 0.18 | — | PDF / Word / Excel 解析 |
| 语音 | Web Speech API + 有道词典音频 | — | 朗读单词、评级播报 |
| 部署 | Netlify | — | 静态托管 |
| Lint | oxlint | 1.75 | 代码检查 |

**注意:** 该项目是纯 Web 技术(React + DOM API),没有任何小程序原生 API。所有 UI、文件解析、TTS、通知都是浏览器 API。

# 二、功能模块清单(8 大模块)

导航在 `src/components/Nav.tsx` 定义 8 个 Tab:首页、导入、卡片、复习、测验、错题、记录、统计。

## 1. 首页 (home)

- 每日目标卡片、今日计划、随机一词
- 入口汇总

## 2. 导入 (upload) — `UploadView.tsx`

- 拖拽/点击上传,支持 Excel / PDF / Word(.docx) / TXT
- 多文件批量导入,每个文件可自动以文件名建词库
- 智能解析:音标提取(`/xxx/` `[xxx]`)、破折号/Tab/多空格分隔
- Excel:A列单词 B列释义 C列音标 D列例句,有表头自动跳过
- 词库(类别)选择/新建

## 3. 卡片 (cards) — `CardsView.tsx`

- 搜索 / 多重筛选(状态·评分·星标·类别) / 排序
- 星标、多选删除
- 遗忘曲线展示
- 单词详情(`WordDetail.tsx`)、编辑(`EditWordDialog.tsx`)
- 类别树管理:`CategoryTreeDialog.tsx` — 支持父子层级、改名、移动、合并、排序、修复孤立释义

## 4. 复习 (review) — `ReviewView.tsx` + `ReadingMode.tsx`

- SM2 间隔重复算法(`src/lib/sm2.ts`):五档评分(0不会/2模糊/3困难/4良好/5简单),ease factor、interval、repetitions、fuzz 抖动
- 卡片状态:new / learning / review / mastered
- 沉浸式阅读模式:自动翻卡 + 发音 + 键盘控制
- 复习计划(`StudyPlanManager.tsx`):按周几 + 时间点 + 类别筛选,定时提醒(用浏览器 `Notification` API,每分钟轮询)

## 5. 测验 (quiz) — `QuizView.tsx`

- 题型:选择题(经典/硬核)/ 拼写(easy/classic 两档难度)/ 词性转换(posconv)/ 错题重默(polysemy)
- 词性转换依赖内置 `wordFamilies.ts`(30KB 词族数据)
- 倒计时、断点续传(quiz_progress 表 / localStorage)、评级语音播报(中文毒舌语音)
- 测验记录写入 quiz_sessions,错题写入 mistakes

## 6. 错题 (mistakes) — `MistakesView.tsx`

- 当前/历史双视图
- 按类别分组
- 原题型重默

## 7. 记录 (history) — `HistoryView.tsx`

- 测验历史记录列表、详情、删除、清空

## 8. 统计 (stats) — `StatsView.tsx`

- 掌握进度(new/learning/review/mastered 占比)
- 复习趋势:周/月/年柱状图
- 遗忘分析:按评分分类 + 分页

## 其他全局功能

- 鉴权:邮箱+密码注册/登录/改密/退出,万能密码登录(`000000`,通过 SQL 函数 `fn_master_login` 临时改密再恢复)
- 每日目标与打卡:streak 连续天数、newWords/reviews 目标
- 学习天数记录:每日自动标记
- 数据备份/恢复:导出/导入 JSON(`repoExportAll` / `repoImportAll`),本地→云端迁移(`repoMigrateLocalToRemote`)
- 暗色模式、响应式布局

# 三、数据库(Supabase / PostgreSQL)

建表脚本在 `supabase/schema.sql`,共 11 张表,全部带 `user_id` + RLS(用户只能访问自己数据):

| 表名 | 用途 | 关键字段 |
|------|------|----------|
| `categories` | 类别(词库)树 | id, name, parent_id(自引用), created_at |
| `words` | 单词 | text, meanings(jsonb 数组), created_at, starred |
| `cards` | SRS 间隔重复卡片 | word_id, ease_factor, interval, repetitions, due_date, last_reviewed, last_quality, total_reviews, correct_reviews, quiz_wrong_count, status |
| `review_logs` | 复习日志 | card_id, word_id, quality, was_correct, ts |
| `sessions` | 每日统计(每用户每天一行) | date, reviewed, correct, learned |
| `mistakes` | 错题 | word_id, category_id, user_answer, correct_answer, mode, timed_out, resolved, spell_difficulty |
| `quiz_sessions` | 测验记录 | date, mode, total, correct, score, label, size, duration, wrongs(jsonb), is_retest |
| `quiz_progress` | 测验断点续传(每用户一行) | state(jsonb), updated_at |
| `user_settings` | 每日目标+打卡(每用户一行) | daily_goal(jsonb), streak_days, last_checkin_date, today_stats(jsonb), today_date |
| `study_plan` | 复习计划(每用户多条) | name, category_ids(jsonb), days_of_week(jsonb), time, enabled |
| `learning_days` | 学习天数(每日记录) | date |

## 关键设计点

- `words.meanings` 是 jsonb 数组,一个单词可属于多个类别,每个释义含 categoryId/meaning/phonetic/example/source
- 本地 Dexie schema 经历 10 个版本迭代(含单词去重合并迁移逻辑)
- 两个 SQL 函数(`fn_master_login` / `fn_restore_password`)实现万能密码,security definer

# 四、迁小程序需要重点关注的差异

1. **UI 框架**:React + Tailwind → 小程序原生 WXML/WXSS 或 Taro/uni-app。Tailwind 不能直接用。
2. **文件解析**:pdfjs-dist / mammoth / xlsx 都是浏览器/Node 库,小程序无法直接跑。导入功能要么放到服务端处理,要么小程序端只支持固定格式粘贴/上传到后端解析。
3. **TTS 语音**:`window.speechSynthesis` 和 `HTMLAudioElement` + 有道音频 URL,小程序需改用 `wx.createInnerAudioContext`(有道 URL 可继续用)或微信同传插件。
4. **通知提醒**:浏览器 `Notification` API → 小程序订阅消息(`wx.requestSubscribeMessage`),逻辑不同。
5. **存储**:IndexedDB(Dexie)在小程序不存在,需用 `wx.setStorageSync` 或继续走 Supabase。
6. **鉴权**:Supabase 邮箱密码登录在小程序里可行(走 HTTP),但万能密码的 RPC 调用也能复用;若想用微信登录需接 Supabase 自定义 Provider 或自建后端换 token。
7. **拖拽上传**:小程序没有拖拽,用 `wx.chooseMessageFile` / `wx.chooseFile`(能力受限)。

数据层 `repo.ts` 已经是「Supabase 优先 + 本地 fallback」的双模式抽象,逻辑层基本可复用,主要是 UI 层和浏览器 API 需要重写。
